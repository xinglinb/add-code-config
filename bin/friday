#!/usr/bin/env node

const program = require('commander');

program
  .version(require('../package').version) // 设置版本
  .usage('<command> [options]')
  .command('add', 'add some code in your project')
  .parse(process.argv); // 参数数组

